# Ingestion schema and contract

Status: Phase 1 schema applied 2026-09-23 as migrations `20260923151159` and `20260923152134`. All new objects are `marquee_*` in `public`. No existing GameDeck object or global default privilege was changed.

Update 2026-09-23: Trakt import and replay succeeded. Dave's anime statuses and scores came from anilist.co through MyAniList for iOS; no MAL account exists, and the unused MAL importer was removed. The AniList import and replay also succeeded: 50 statuses, 33 ratings and 50 stable media mappings with no open AniList review. AniList did not seed dated episode watch events. The Crunchyroll runner choice is open; the n8n reference in the project context remains a proposal, not an approved activation.

The forward, Marquee-only migration allowing `source='anilist'` in `marquee_statuses` and `marquee_ratings` was applied and verified before the seed. Do not rewrite the applied migration or touch GameDeck. Leave the historical unused MAL values in the applied schema until a separately reviewed cleanup.

The applied migration and importer live at `supabase/migrations/20260923183118_marquee_anilist_user_state_source.sql` and `scripts/anilist-import/`. The script requests scores on AniList's fixed 10-point decimal scale and retains the raw entry. Dave approved the public-list import for `Daveywavey9`. Muse reviewed the 45 initially ambiguous entries, wrote manual mappings, then replayed the import to zero unresolved entries. The importer is a one-time list seed, not an ongoing episode history source.

| Layer | Tables | Key invariant |
| --- | --- | --- |
| Catalog | `marquee_titles`, `marquee_shows`, `marquee_movies`, `marquee_seasons`, `marquee_episodes` | One top-level title is a show or movie; seasons belong to shows and episodes to seasons. |
| User state | `marquee_watch_history`, `marquee_watch_history_sources`, `marquee_statuses`, `marquee_ratings`, `marquee_watchlist` | Each row belongs to an `auth.users` ID; history observations from two providers can point to one canonical event. |
| Identity | `marquee_source_mappings`, `marquee_franchise_overrides`, `marquee_mapping_review` | `(source, source_entity_type, source_id)` has one canonical target. Manual locks win; only one open review per source identity. |
| Operations | `marquee_ingest_raw`, `marquee_sync_state`, `marquee_sync_runs` | Raw dedupe by `(scope, source, entity type, dedupe key)`; checkpoint is independent of run telemetry. |

## Choices to preserve

- UUID canonical IDs, text stable source IDs. Provider IDs never become primary keys. Catalog IDs are immutable once assigned.
- `marquee_source_mappings` uses one typed target FK column at a time. This enforces real FK integrity despite multiple canonical entity types.
- Anime AniList entries normally map to `season`; franchise overrides explicitly map a stable source identity to a `show` title. Episode number and season number can be unknown; source IDs still prevent duplicates.
- Statuses and ratings accept either a title or a season target. This retains distinct AniList anime season scores. Watchlist stays at title level.
- Canonical history stores one row per logical viewing and a stable logical key. `marquee_watch_history_sources` links every source event by stable ID. Raw payloads remain available even before mapping.
- Raw payloads may be re-fetched and hashed; `dedupe_key` is stable across replays, while a changed payload updates the stored version during a future adapter phase. Sensitive tokens and request headers must never be stored in payload.
- Sync runs are one row per execution: an initial `running` row can transition once to a terminal status. Ingestion cannot delete run rows; an Auth user deletion may cascade to remove personal records.
- `scope_key` is `catalog` for shared metadata or the owner UUID as text for private fetches. It is checked against `user_id`.
- All `marquee_*` tables have RLS enabled. Catalog SELECT is available to authenticated users; user state uses `(select auth.uid()) = user_id`; operational and identity tables are service-role only. Anonymous has no grants. `service_role` owns ingestion access.

## Adapter transaction rule

Fetch pages completely, persist raw records, resolve mappings or review entries, upsert canonical rows idempotently, then mark the run successful and advance the checkpoint in **one final database transaction**. Any future Crunchyroll runner must supply this atomicity, through a Marquee-scoped RPC or an equivalent transactional adapter. A run with an unexpected page shape, missing page, failed persistence or incomplete count remains failed without a checkpoint advance. A review item is a valid terminal normalization outcome only after raw and review writes succeed. Replays use stable source IDs, dedupe keys and logical event keys.

Before the first real import, validate the Crunchyroll/Trakt overlap window with actual source timestamps. Do not guess that window in this schema migration.

## Phase 2 and 3 gates

### Agreed execution order as of 2026-09-23

1. Establish Trakt credential continuity, including the expiring access token and flagged client-secret rotation, then schedule and verify the existing idempotent sync. Rotate the flagged Supabase database password through the operator without committing secrets. The manual Trakt import already works; this step makes it ongoing.
2. Test Crunchyroll history access read-only. Establish pagination, oldest available event, timestamps, stable episode IDs, account/profile scope and whether the returned history is complete. Do not promise lifetime coverage before checking. Select the extraction method and runner after this evidence; n8n has not been conclusively ruled out in this repo plan.
3. Backfill all accessible Crunchyroll episode history. Preserve raw observations, map stable IDs to the reviewed AniList catalog, send ambiguity to review, check Trakt overlap with real timestamps, and verify replay before committing a success checkpoint.
4. Start incremental Crunchyroll sync with a replay overlap at the backfill boundary. Verify fresh events, no duplicates, failure behavior, source provenance and an unchanged GameDeck baseline. Do not activate a schedule or live n8n workflow without Dave's authorization.

Trakt token refresh and a full Marquee-token database replay succeeded on 2026-09-23. The daily GitHub Actions schedule is set for 11:17 UTC; verify the first scheduled execution before treating Trakt as ongoing. AniList is complete as a one-time status and score seed, and MAL is out of scope. The manual, read-only [Crunchyroll history probe](../scripts/crunchyroll/README.md) established observed feed coverage without DB access or raw record logging. Its GitHub Actions runner is only for this probe; the ongoing Crunchyroll runner remains undecided. Only after the ongoing feeds are proven should PWA work begin.

Crunchyroll coverage update 2026-09-23: [manual run 35932361388](https://github.com/ddibara5/marquee/actions/runs/35932361388) completed using the older `content/v1/watch-history` endpoint's `next_page` links. It reached an end marker after 397 pages (last page 13), with 7,933 distinct event IDs and observations, from 2023-10-09T00:10:48Z to 2026-09-23T02:58:23Z. All 1,000 v2 event IDs appear in v1 with identical `date_played`; v1 contains another 6,933 event IDs. All 609 v2 records with panel IDs agree with v1. The newer endpoint returned ten full 100-record pages, then HTTP 400 on page 11 even at a smaller page size; it is not a complete-history source. The v1 feed had 5,979 observations with a panel episode ID and 1,954 without a panel. All events have event and parent IDs, but the missing-panel events have only seven distinct parent IDs; none of the 5,979 records with panels has a parent ID equal to its panel ID. Do not treat parent IDs as episode IDs. All `date_played` values parsed, but real-world timestamp accuracy, selected-profile scope and lifetime coverage remain unverified. The probe stores nothing and is not an importer.

Proposed first backfill gate: Dave confirms the intended website profile and a few recent/older dates. Use v1 `next_page` traversal, require a terminal link, 7,933 distinct event IDs as this run's comparison baseline (allow newer entries), and fail on repeated pages, unexpected shape or count regression. Preserve each source event in `marquee_ingest_raw` keyed by its event ID. For records with `panel.id`, resolve through a locked Crunchyroll episode mapping first; propose new mappings only using corroborated series/season/episode identifiers and the reviewed AniList catalog. Never use title text alone as a durable identity. Keep conflicts and ambiguous anime versions in `marquee_mapping_review`. For records without a panel, retain raw observations and open review keyed by event ID; do not create a canonical episode watch from the seven parent IDs. Compare actual Trakt and Crunchyroll canonical episode IDs and playback timestamps in a read-only overlap analysis before selecting a deterministic deduplication window. Preserve both source links when one logical viewing is proven; leave uncertain events separate or in review. Replays must not duplicate watches. Present the profile, overlap and review counts for approval before any live backfill or Supabase mutation. Select and authorize a permanent runner separately.

AniList overlap gate: A read-only database check on 2026-09-23 found 47 approved AniList season mappings and 3 film mappings, with 50 statuses and 33 ratings, but no AniList episode watches. Trakt's current show/film mappings and watched episodes have zero shared canonical AniList season/film targets. The [separate manual overlap workflow](../.github/workflows/crunchyroll-catalog-overlap.yml) measures Crunchyroll series/season title candidates against those approved AniList catalog entries without storing source records or emitting titles/IDs. A unique title candidate remains review evidence; inspect corroborating episode/season identifiers and resolve franchise versions before writing a stable Crunchyroll-to-canonical mapping. This comparison cannot establish watched-at overlap with Trakt or confirm profile scope.

AniList comparison result: [manual run 35933539575](https://github.com/ddibara5/marquee/actions/runs/35933539575) read all 7,933 Crunchyroll observations. Among 222 source seasons with 5,979 episode-panel observations, 135 seasons (2,654 observations) had one AniList title candidate, 11 (127) had multiple, 19 (436) had conflicting season/show title evidence, and 57 (2,762) had none. The other 1,954 observations had no episode metadata. No identifiable movie panels were available to compare against the three AniList films. These are candidate counts, not confirmed cross-provider matches: many source seasons or language versions may point to one AniList season. For review, group by stable Crunchyroll series and season IDs, retain the AniList media ID and canonical season target as candidate context, verify season/episode numbering and franchise relations, then lock only reviewed mappings. Missing-panel events remain raw-only until independently identified. Any Trakt event deduplication requires a subsequent canonical episode and timestamp check; do not infer it from title candidates.

Identity review update: Dave confirmed the cookie came from his intended Crunchyroll profile. [Read-only run 35934892346](https://github.com/ddibara5/marquee/actions/runs/35934892346) found 5,979 distinct episode panel IDs across 222 source seasons. Of those, 5,966 had usable episode numbers; 13 did not. No source season had multiple panel IDs for one episode number. Some 1,668 metadata `identifier` values were shared across panel IDs, always within the same Crunchyroll series and episode number but across source season IDs and audio locales. This is evidence of possible language versions, not permission to merge automatically. The 135 source seasons with one AniList title candidate pointed to only 30 distinct AniList targets; 15 targets attracted multiple source seasons. Read-only DB checks found zero approved Crunchyroll episode mappings or Crunchyroll history links, 2,443 Trakt episode watches, and zero Trakt watches on AniList-mapped seasons. There are therefore no confirmed cross-provider episode pairs for a timestamp match yet. Resolve version/season mappings using source IDs and the AniList relation graph first; then compare episode-level timestamps before any cross-source deduplication. The temporary push trigger used for this probe was removed after the runs; the workflow remains manual-only.

Runner direction (Dave, 2026-09-23): prefer n8n long term if usage stays modest. The connected n8n instance supports workflow execution and inspection, but has no Marquee or Crunchyroll credential configured. Read-only execution metadata showed 443 runs in the preceding 30 days and 54–63 on each of the two most recent UTC days; these are observations, not the plan's exact billing balance. Use **one daily n8n schedule execution** (about 30/month), dispatching a Marquee-only GitHub Actions worker through a repository-scoped credential with Actions write access. Keep the Crunchyroll cookie and database URL only in GitHub Actions, the pagination and idempotent atomic transaction in the Python worker, and n8n's input/output to a run identifier and aggregate status. Avoid per-page n8n executions, a separate polling workflow, and stored private history in n8n execution data. The one-time backfill should run in the worker after approved mapping gates; later incremental runs should replay a bounded overlap and advance the checkpoint only on success. A dedicated Marquee RPC/worker can replace the Actions worker later without changing n8n's one-execution cadence. Verify the actual n8n subscription headroom and workflow execution-data settings before activation. No live Marquee n8n workflow or schedule exists yet.

Trakt: paginate history, ratings and watchlist; retain Trakt, TMDb and IMDb IDs, original timestamps and run heartbeat. AniList account: fetch the complete anime list, retain stable media/list-entry IDs and source score format, seed statuses and scores with source precedence. Crunchyroll: cache mapped IDs, throttle AniList on cache misses, back off on HTTP 429, and fail loudly on contract changes. Credentials are supplied at runtime; no live Crunchyroll workflow is authorized yet.

Phase 2 Trakt script: [`scripts/trakt-sync/README.md`](../scripts/trakt-sync/README.md) documents the Python implementation, direct OAuth transport, reviewed non-anime TV ID gate and private Postgres runner requirements. Its first live import and replay succeeded on 2026-09-23. The bridge interface has not been provided; source access can be swapped without changing the catalog contract.

Read-only import gate (2026-09-23): `.github/workflows/crunchyroll-import-gate.yml` checks complete v1 pagination, unique event IDs, parseable playback dates, consistent panel identity, the 7,933 event floor, the verified oldest event boundary, current Crunchyroll mapping/link counts and GameDeck regression counts. It emits aggregates only. A successful gate does not resolve missing panels, episode numbers, language versions, AniList identity or Trakt timestamp overlap. Until mappings are reviewed and an import worker's replay and rollback behavior are verified, zero Crunchyroll events qualify for automatic canonical insertion. Do not activate the n8n schedule or run a live write from this gate.

Raw-only staging worker (prepared, not run): `scripts/crunchyroll/stage_history.py` and the manual-only `crunchyroll-stage-history` workflow default to read-only. The explicit `stage=true` option stores each complete source observation by stable event ID in `marquee_ingest_raw`, opens season/version review groups and individual review for missing panels and invalid episode numbers, and advances a Marquee-only checkpoint in one transaction. It rejects lost prior event IDs, changed event dates/panel IDs, a changed source account, preexisting Crunchyroll mappings or watch links, and incomplete pages. A replay updates existing raw keys without adding duplicates or reopening resolved reviews. It never creates canonical episodes or watches and never emits private records. Do not select `stage=true` until Dave reviews the verified 7,933-event coverage, 1,954 missing panels, 13 invalid episode numbers, mapping plan, and the private storage implications. After approved staging, resolve source series/season/version/episode identities against AniList candidates; compare confirmed watches to Trakt timestamps before canonical insertion. The n8n schedule remains off.

Raw stage result 2026-09-24: [manual run 35939187553](https://github.com/ddibara5/marquee/actions/runs/35939187553) succeeded. The committed database has 7,933 raw Crunchyroll observations with 7,933 distinct event keys and all 7,933 marked for review. It opened 2,189 review records: 222 source seasons, 1,954 events without an episode panel, and 13 panels without a usable episode number. All review identities have raw backing records. Source dates span 2023-10-09T00:10:48Z to 2026-09-23T02:58:23Z; none are missing. The run and checkpoint agree on 7,933. There are zero Crunchyroll catalog mappings, canonical watches, and watch source links. GameDeck counts remain `game_ranks=53` and `rank_comparisons=154`. This is source storage and review preparation, not a canonical backfill.

Read-only version/duplication analysis of staged data: all 5,979 episode panel IDs appear in their episode `versions` arrays; the 5,979 panel IDs form 3,130 disjoint version sets, matching 3,130 metadata identifiers. Of those sets, 1,668 have multiple watched panel IDs spanning source season IDs and audio locales; none crosses source series, episode number, or metadata identifier. The 5,979 panel observations produce 3,479 distinct source episode-and-playback-timestamp groups. In 1,499 groups, multiple version panel IDs share the exact timestamp, accounting for 2,500 extra raw observations. Each such group agrees on source parent ID and `fully_watched`, although 1,253 have different playhead values. This is strong evidence of version-related duplicate history observations, but is not an approved canonical watch merge. Of the 3,479 groups, 126 are marked partially watched and 3,353 fully watched. The 1,954 panel-less events remain separately unresolved. Preserve all source event IDs as provenance; review version groups and partial-playback semantics before making any canonical watches. No Trakt timestamp deduplication is possible yet because no Crunchyroll episodes have approved canonical mappings. Leave the n8n scheduler inactive.

Next read-only gate: `scripts/crunchyroll/staged_review.py` checks whether the staged source version GUID sets consistently identify one source episode, counts title and AniList relation candidates across all observed language versions, and measures Trakt timestamp proximity only where an approved Crunchyroll episode mapping already exists. It does not produce mappings from titles or AniList relations. Source episodes without panels remain outside these candidate groups; partially watched events remain separate. The workflow is manual-only and emits only aggregate counts. Review the output before resolving any stable source mapping or writing canonical watches.

Staged identity review result: [read-only run 35941093515](https://github.com/ddibara5/marquee/actions/runs/35941093515) succeeded against the committed 7,933-event snapshot. Source GUID lists and metadata identifiers agree on 3,130 episode version groups across 5,979 panel IDs. The 3,479 source episode/timestamp groups include 1,499 same-time multi-version groups and 2,500 extra source observations; 3,353 groups are fully watched and 126 partial. AniList title evidence at the source episode version-group level: 1,362 groups have one consistent title candidate across all observed versions, 1,515 have no title candidate, 126 have multiple candidates, and 127 have conflicting title evidence. Among ambiguous groups, 38 have candidates joined by an AniList relation edge. These are review leads, not confirmed identity mappings. There are zero approved Crunchyroll episode mappings and therefore zero confirmed Crunchyroll/Trakt episode pairs for timestamp comparison despite 2,443 Trakt episode watches. A read-only database check after the run still found 7,933 raw observations, zero Crunchyroll mappings and canonical watches, and GameDeck baselines 53/154. The temporary push trigger is removed after this run; the review workflow remains manual-only.

Private season candidate queue prepared (not applied): `scripts/crunchyroll/season_review_queue.py` derives AniList title and relation evidence for the 222 stable Crunchyroll season IDs. Its default mode is read-only and prints only category counts. The explicit `apply_review=true` option updates only the existing open `marquee_mapping_review` season rows: source season title, review reason, and candidate JSON containing canonical season ID, AniList media ID, title evidence and related-candidate flag. Every candidate is marked review-only. Before updating, a single transaction takes the Crunchyroll advisory lock and checks the staged checkpoint, all event IDs, the exact open season review set, and zero approved Crunchyroll mappings. It writes no source mappings, episodes, watches or Trakt links. The option is not authorized for live use until Dave reviews this exact candidate queue plan; approval of raw staging did not approve season candidate population. Title candidates, including 135 single-candidate source seasons, remain unconfirmed until season/version and episode correspondence are reviewed.

Read-only season queue preview: [run 35941539423](https://github.com/ddibara5/marquee/actions/runs/35941539423) validated 222 source season review rows and 307 total AniList title candidates. Category counts were 135 seasons with one candidate, 11 with multiple, 19 with conflicting title evidence, and 57 with none. This preview wrote zero mappings and zero canonical watches, and did not populate the private review queue. The `apply_review=true` option is still pending review. Its eventual write would update only the 222 existing open season review rows with source season titles and candidate metadata. No raw event content or playback dates are written to candidate JSON.
